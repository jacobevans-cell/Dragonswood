const app=document.querySelector('#app');
const toast=document.querySelector('#toast');
const dialogRoot=document.querySelector('#dialog-root');
let integrationController=null;
let integrationSession={status:'loading',message:'Loading Dragonswood teacher identity…'};
let teacherName='Mr. Evans';
const arcadeTeacher=window.DWV33ArcadeTeacher;

const navItems=[
 ['student-command','🪄','Student Command'],['gradebook','📊','Gradebook'],['scribe','📜','Scribe Command'],['rewards','🎯','Class Rewards'],['passes','🎟️','Pass Control'],['jobs','🧹','Guild Jobs'],['schedule','🕘','Schedule'],['tools','🧰','Classroom Tools'],['leaderboards','🏆','Leaderboards']
];
const arcadeNav=['arcade','🕹️','Arcade Time'];
function teacherNavItems(){return arcadeTeacher?.enabled?[...navItems,arcadeNav]:navItems}
function arcadeEnvironmentLabel(){return window.DWV33Integration?.environment==='manual-preview'?'Local preview state only':'Fictional emulator writes'}
let students=[];
const state={
 page:'student-command',selected:new Set(),commandTab:'Rewards',classPoints:64,universalPoints:24,
 activePasses:[{name:'Alaina',kind:'Bathroom',time:'1 min ago'}],pendingPasses:[{name:'Caleb',kind:'Water',time:'Now'},{name:'Nala',kind:'Bathroom',time:'2 min ago'}],
 payrollApproved:false,scheduleDay:'Today',gradeFilter:'All',scribeActive:false,leaderRange:'This Week',
 arcadePeriodId:'period-1',arcadeRows:{},arcadeBusy:false
};
const references={
 'student-command':'assets/reference/teacher/dragonswood-teacher-02-gradebook.jpg',
 'gradebook':'assets/reference/teacher/dragonswood-teacher-01-student-command.jpg',
 'scribe':'assets/reference/teacher/dragonswood-teacher-04-class-rewards.jpg',
 'rewards':'assets/reference/teacher/dragonswood-teacher-05-pass-control.jpg',
 'jobs':'assets/reference/teacher/dragonswood-teacher-06-guild-jobs.jpg',
 'schedule':'assets/reference/teacher/dragonswood-teacher-07-schedule.jpg',
 'tools':'assets/reference/teacher/dragonswood-teacher-08-classroom-tools.jpg',
 'leaderboards':'assets/reference/teacher/dragonswood-teacher-09-leaderboards-final.jpg'
};
function showToast(message){toast.textContent=message;toast.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>toast.classList.remove('show'),2400)}
function openDialog(title,body,actions=''){dialogRoot.innerHTML=`<div class="dialog-backdrop" data-dialog-backdrop><section class="dialog" role="dialog" aria-modal="true" aria-labelledby="dialog-title"><h2 id="dialog-title">${title}</h2>${body}<div class="dialog-actions">${actions||'<button class="btn btn-primary" data-close-dialog>Done</button>'}</div></section></div>`;dialogRoot.querySelector('[data-close-dialog]')?.addEventListener('click',closeDialog);dialogRoot.querySelector('[data-dialog-backdrop]')?.addEventListener('click',e=>{if(e.target===e.currentTarget)closeDialog()});dialogRoot.querySelector('button')?.focus()}
function closeDialog(){dialogRoot.innerHTML=''}
function escapeHtml(value){return String(value??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}
function currentPage(){const h=location.hash.replace('#','');return teacherNavItems().some(n=>n[0]===h)?h:'student-command'}
function navMarkup(){return `<nav class="teacher-nav" aria-label="Teacher portal">${teacherNavItems().map(([id,icon,label])=>`<button class="nav-link ${state.page===id?'active':''}" data-page="${id}" ${state.page===id?'aria-current="page"':''}><span class="nav-icon">${icon}</span><span class="nav-main">${label}</span></button>`).join('')}</nav>`}
function shell(){const preview=window.DWV33Integration?.environment==='manual-preview';return `<div class="portal teacher-shell teacher-page-${state.page}" data-tester-build="v3.3"><aside class="teacher-sidebar"><div class="teacher-brand"><div class="teacher-brand-row"><img class="teacher-command-crest" src="assets/art/teacher-command-crest.png" alt=""><div><h2>TEACHER PORTAL</h2><p>Explore Academy</p></div></div></div>${navMarkup()}<div class="prototype-badge">✦ <b>V3.3 SAFE TESTER</b><small>${arcadeTeacher?.enabled?arcadeEnvironmentLabel():'Local mock data only'}</small></div></aside><main class="teacher-main" id="page-content"><header class="teacher-topbar"><div class="teacher-header-inner"><div><div class="teacher-header-caption">EXPLORE ACADEMY · EVANS 4–5 · TEACHER PORTAL</div><div class="teacher-command-lockup"><img class="teacher-command-crest" src="assets/art/teacher-command-crest.png" alt=""><h1>DRAGONSWOOD COMMAND</h1></div></div><div class="teacher-account"><span class="online-dot"></span><span>${escapeHtml(teacherName)}</span><span>⌄</span></div></div></header><div class="teacher-content">${pageMarkup()}</div></main>${referenceButton()}<div class="tester-ribbon">${arcadeTeacher?.enabled?preview?'V3.3 ARCADE • LOCAL PREVIEW':'V3.3 ARCADE • EMULATOR WRITES':'V3.3 TESTER • NO LIVE WRITES'}</div></div>`}
function referenceButton(){return new URLSearchParams(location.search).get('reference')==='1'?`<button type="button" class="btn btn-gold btn-sm reference-button" data-reference>Reference</button>`:''}
function pageBanner(icon,eyebrow,title,sub,action=''){return `<section class="teacher-page-banner"><div class="teacher-page-title"><div class="teacher-page-icon">${icon}</div><div><div class="eyebrow">${eyebrow}</div><h2>${title}</h2><p>${sub}</p></div></div>${action}</section>`}
function metric(icon,label,value,sub){return `<article class="panel metric"><div class="metric-icon">${icon}</div><div><div class="eyebrow">${label}</div><strong>${value}</strong><small>${sub}</small></div></article>`}
function pageMarkup(){switch(state.page){case'gradebook':return gradebookPage();case'scribe':return scribePage();case'rewards':return rewardsPage();case'passes':return passesPage();case'jobs':return jobsPage();case'schedule':return schedulePage();case'tools':return toolsPage();case'leaderboards':return leaderboardsPage();case'arcade':return arcadePage();default:return studentCommandPage()}}

function studentCommandPage(){return `<section class="panel attention-strip"><div class="attention-item"><span class="metric-icon">⭐</span><div><b>Recognition requests</b><small>2 waiting</small></div><span class="gold attention-action">REVIEW</span></div><div class="attention-item"><span class="metric-icon">🔒</span><div><b>Daily access</b><small>Whole class unlocked</small></div><span class="gold attention-action">MANAGE</span></div><div class="attention-item"><span class="metric-icon">✓</span><div><b>Curriculum overrides</b><small>None waiting</small></div></div><div class="attention-score"><strong>48</strong><small>CLASS HP</small></div><div class="attention-score"><strong>20</strong><small>LEVEL CAP</small></div><div class="attention-score"><strong>150</strong><small>DAILY XP</small></div></section>
<section class="teacher-workspace"><article class="panel step-card"><div class="step-head"><div><div class="eyebrow">Step 1</div><h3>Choose Students</h3></div><span class="selected-badge">${state.selected.size} selected</span></div><div class="search-row"><input class="search-input" id="student-search" placeholder="⌕ Search the guild roster" aria-label="Search students"></div><div class="roster-filters"><button class="filter-mini active">All</button><button class="filter-mini">4th</button><button class="filter-mini">5th</button><button class="filter-mini">Boys</button><button class="filter-mini">Girls</button></div><div class="row between mb-12"><button class="btn btn-quiet btn-sm" data-select-visible>Select visible</button><button class="btn btn-quiet btn-sm" data-clear-selected>Clear</button></div><div class="roster-list" id="roster-list">${rosterRows(students)}</div></article><article class="panel command-panel"><div class="step-head step-head-pad"><div><div class="eyebrow">Step 2</div><h3>Choose a Command</h3></div><span class="muted text-9">Selection stays active while you switch tools</span></div><div class="teacher-tabs">${['Rewards','Corrections','Custom','Management'].map(t=>`<button class="teacher-tab ${state.commandTab===t?'active':''}" data-command-tab="${t}">${tabIcon(t)} ${t}</button>`).join('')}</div><div class="command-body">${commandCards()}</div></article></section>
<div class="review-dock"><div><div class="eyebrow">Ready to review</div><strong>${state.selected.size? 'Choose a command':'Select scholars first'}</strong></div><div class="row"><span class="muted text-10">${state.selected.size} students selected</span><button class="btn btn-primary" data-review ${state.selected.size?'':'disabled'}>REVIEW CHANGES</button></div></div>`}
function tabIcon(t){return({Rewards:'🏆',Corrections:'⚖️',Custom:'⚙️',Management:'🧙'})[t]}
function rosterRows(list){return list.map(s=>`<label class="roster-row ${state.selected.has(s.id)?'selected':''}" data-roster-name="${escapeHtml(s.name)}"><input class="roster-check" type="checkbox" ${state.selected.has(s.id)?'checked':''} data-student="${escapeHtml(s.id)}"><span class="roster-avatar">${escapeHtml(s.name[0]||'?')}</span><span><span class="roster-name">${escapeHtml(s.name)}</span><span class="roster-meta">${escapeHtml(s.meta)}</span></span></label>`).join('')}
function selectedNames(){const ids=state.selected;return students.filter(s=>ids.has(s.id)).map(s=>s.name)}
function commandCards(){const cards={Rewards:[['✅','Ready & Working','Materials out, started quickly, focused.','+15 XP • +3 Gold'],['🚶','Excellent Transition','Quiet, quick, ready at the next location.','+10 XP • +2 Gold'],['🍎','Great Lunch Behavior','Respectful and responsible lunch choices.','+20 XP • +4 Gold'],['🎨','Great Specials Behavior','Represented our class well during specials.','+20 XP • +4 Gold'],['📚','Outstanding Effort','Persistence, improvement, or excellent focus.','+30 XP • +6 Gold'],['🛡️','Dragonswood Leadership','Helped the class through a brave, kind choice.','+40 XP • +8 Gold']],Corrections:[['↩️','Reset Focus','Remove a small amount of daily XP.','−10 XP'],['⏳','Pause Access','Temporarily pause one optional feature.','No stat change'],['🧭','Teacher Check-In','Require a teacher conversation before an optional activity.','Status only'],['📝','Reflection Needed','Assign a short reflection checkpoint.','Status only']],Custom:[['✨','Custom XP Change','Choose a specific XP amount.','Teacher entered'],['🪙','Custom Gold Change','Choose a specific Gold amount.','Teacher entered'],['❤️','Custom HP Change','Adjust classroom HP carefully.','Teacher entered'],['🏷️','Recognition Note','Record a positive teacher note.','No stat change']],Management:[['🔓','Unlock Daily Access','Restore daily optional destinations.','Access only'],['🔒','Pause Optional Area','Pause one optional destination.','Access only'],['👤','Profile Review','Open scholar profile and history.','Read only'],['📌','Teacher Flag','Add a teacher-only follow-up marker.','Teacher only']]};return `<div class="section-heading no-top"><div><div class="eyebrow">${state.commandTab==='Rewards'?'Positive recognition':'Teacher tools'}</div><h2>${state.commandTab==='Rewards'?'Quick Rewards':state.commandTab}</h2></div></div><div class="command-grid">${cards[state.commandTab].map(c=>`<button class="command-card" data-command="${c[1]}" type="button"><span class="text-22">${c[0]}</span><h4>${c[1]}</h4><p>${c[2]}</p><div class="command-effect">${c[3]}</div><span class="command-add">＋</span></button>`).join('')}</div>`}

function gradebookPage(){
  const grades=[['Abigail','A','94','98','91','93','2','5'],['Alaina','A','87','86','84','90','—','4'],['Alejandro','A','91','95','88','89','—','5'],['Aliya','A','78','74','80','82','—','5'],['Caleb','C','83','89','77','84','—','5'],['Christian','C','96','98','91','93','1','4'],['Damian','D','72','86','84','90','—','4'],['DragonTest','D','88','95','88','89','2','5']];
  const filtered=state.gradeFilter==='All'?grades:grades.filter(g=>g[7]===state.gradeFilter);
  return `${pageBanner('📚','Academic records','Dragonswood Gradebook','See the class first. Open one scholar only when you need assignment details.','<button class="btn btn-primary btn-sm" data-toast="Gradebook refreshed from mock data.">↻ Refresh</button>')}<section class="metric-grid">${metric('👥','Students','22','12 fourth • 10 fifth')}${metric('✦','Class average','86%','B • up 3%')}${metric('📜','Assigned work','18','Across 3 categories')}${metric('⚠️','Actual missing','7','5 students affected')}</section><section class="teacher-workspace"><aside class="panel weight-panel"><div class="eyebrow">GRADE SETTINGS</div><h3>Category Weights</h3>${[['Daily Quest',40],['Curriculum Quest',40],['Reading Tests',20]].map(w=>`<div class="weight-row"><div class="weight-label"><span>${w[0]}</span><b class="gold">${w[1]}%</b></div><progress class="dw-progress dw-progress-weight" max="100" value="${w[1]}" aria-label="${w[0]} weight">${w[1]}%</progress></div>`).join('')}<button class="btn btn-secondary w-full">Edit Category Weights</button><div class="legend"><div class="eyebrow">QUICK LEGEND</div><div><i class="legend-a"></i>90–100 A</div><div><i class="legend-b"></i>80–89 B</div><div><i class="legend-c"></i>70–79 C</div><div><i class="legend-d"></i>Below 70</div></div></aside><article class="panel gradebook-panel"><div class="gradebook-toolbar"><input class="search-input" placeholder="⌕ Search students" aria-label="Search students"><div class="grade-filter-segments" aria-label="Grade filter">${[['All','All grades'],['4','4th'],['5','5th']].map(([v,label])=>`<button type="button" class="${state.gradeFilter===v?'active':''}" data-grade-segment="${v}">${label}</button>`).join('')}</div><button class="btn btn-secondary btn-sm" data-toast="CSV export simulated.">↓ Export CSV</button></div><table class="grade-table"><thead><tr><th>Scholar</th><th>Total</th><th>Daily</th><th>Curriculum</th><th>Reading</th><th>Missing</th></tr></thead><tbody>${filtered.map(g=>`<tr><td><div class="student-cell"><span class="roster-avatar">${g[1]}</span><span><b>${g[0]}</b><small class="roster-meta">Grade ${g[7]} • ${['Abigail','Alaina','Aliya','DragonTest'].includes(g[0])?'Girls':'Boys'}</small></span></div></td><td><span class="grade-score ${gradeClass(g[2])}">${g[2]}%</span></td><td>${g[3]}%</td><td>${g[4]}%</td><td>${g[5]}%</td><td>${g[6]==='—'?'—':`<span class="missing-pill">${g[6]}</span>`}</td></tr>${g[0]==='Alaina'?`<tr class="grade-detail-row"><td colspan="6"><div class="grade-detail"><div class="grade-detail-head"><div><small>LATEST ASSIGNMENT</small><b>Daily Quest • Day 14</b></div><div><small>SCORE</small><b>86/100</b></div><div><small>TEACHER ACTION</small><button class="btn btn-quiet btn-sm" data-toast="Assignment list opened in tester mode.">View All Assignments →</button></div></div></div></td></tr>`:''}`).join('')}</tbody></table></article></section>`;
}
function gradeClass(v){const n=Number(v);return n>=90?'grade-a':n>=80?'grade-b':n>=70?'grade-c':'grade-d'}

function scribePage(){return `${pageBanner('✍️','Writing missions','Scribe Arena Command','Launch a mission, watch scholars write, and review everything from one calm workspace.',state.scribeActive?'<button class="btn btn-secondary btn-sm" data-close-mission>Close mission</button>':'<span class="selected-badge">○ No active mission</span>')}<section class="split-main"><article class="panel teacher-form"><div class="eyebrow">Step 1</div><h3>Build the Mission</h3><div class="form-grid"><div class="field"><label>Mission title</label><input value="Morning Quickwrite"></div><div class="field"><label>Mode</label><select><option>Quickwrite</option><option>Draft</option></select></div><div class="field"><label>Writing type</label><select><option>Narrative</option><option>Opinion</option><option>Informative</option></select></div><div class="field"><label>Target skill</label><select><option>Sensory Details</option><option>Strong Evidence</option></select></div><div class="field"><label>Time</label><input value="5"></div><div class="field"><label>Minimum</label><input value="75"></div><div class="field span-3"><label>Prompt</label><textarea>Describe a place that feels mysterious. Use at least three sensory details.</textarea></div></div><div class="row mt-12"><button class="btn btn-primary" data-launch-scribe ${state.scribeActive?'disabled':''}>🔥 Launch to Class</button><button class="btn btn-secondary" data-close-mission>Close Mission</button></div></article><aside class="panel writing-status"><div class="eyebrow">Step 2</div><h3>Live Writing Status</h3><div class="empty-status"><span class="text-26">📜</span><div><div class="eyebrow">${state.scribeActive?'Live':'Waiting'}</div><b>${state.scribeActive?'Mission active':'No active mission'}</b><div class="muted text-9">${state.scribeActive?'Students can begin writing now.':'Launch a writing mission to begin.'}</div></div></div><div class="donut"><span>${state.scribeActive?'4':'0'}<small class="donut-caption">of 22 submitted</small></span></div><div class="status-stats"><div class="status-stat"><b>${state.scribeActive?'8':'0'}</b><small class="roster-meta">Drafting</small></div><div class="status-stat"><b>${state.scribeActive?'2':'0'}</b><small class="roster-meta">AI scored</small></div><div class="status-stat"><b>${state.scribeActive?'104':'0'}</b><small class="roster-meta">Avg words</small></div></div></aside></section><article class="panel responses-panel"><div class="eyebrow">Step 3</div><h3>Student Responses</h3>${state.scribeActive?`<div class="stack mt-12">${['Abigail','Joshua','Alaina','Christian'].map((n,i)=>`<div class="pass-card"><div class="pass-row"><div class="pass-student"><span class="roster-avatar">${n[0]}</span><div><b>${n}</b><p>${i<2?'Submitted':'Drafting'} • ${89+i*12} words</p></div></div><button class="btn btn-secondary btn-sm" data-toast="Response opened for review.">Review</button></div></div>`).join('')}</div>`:'<div class="empty-center"><div><div class="gold text-28">✦</div><strong>The scriptorium is quiet.</strong><small>Student responses will appear here after you launch the mission.</small></div></div>'}</article>`}

function rewardsPage(){return `${pageBanner('🛡️','Whole-class motivation','Class Rewards & Goals','See every point bank and milestone without scrolling through separate control panels.','<button class="btn btn-primary btn-sm" data-toast="Milestone editor opened.">＋ Add Milestone</button>')}<article class="panel class-points"><div class="class-point-number"><span class="trophy">🏆</span><div><div class="eyebrow">Shared class points</div><strong>${state.classPoints}</strong><small class="roster-meta">${75-state.classPoints} points until the next class milestone</small></div></div><div><progress class="dw-progress dw-progress-class" max="100" value="${Math.min(100,state.classPoints/75*100)}" aria-label="Class points goal progress">${Math.min(100,state.classPoints/75*100)}%</progress></div><div class="class-point-controls"><input class="search-input" placeholder="Reason (optional)"><button class="btn btn-secondary btn-sm" data-points="1">+1</button><button class="btn btn-secondary btn-sm" data-points="5">+5</button><button class="btn btn-secondary btn-sm" data-points="10">+10</button><button class="btn btn-secondary btn-sm" data-points="-1">−1</button><button class="btn btn-secondary btn-sm" data-toast="Last local point change undone.">Undo</button></div></article><section class="grid-3 mt-12">${goalCard('🌤️','Daily goal','Second Recess','8 / 10',80)}${goalCard('🐾','Long-term goal','Class Pet','172 / 250',69)}${goalCard('🚌','Long-term goal','Field Trip','418 / 750',56)}</section><section class="grid-2 mt-12"><article class="panel flex-bank"><span class="star">🌟</span><div><div class="eyebrow">Universal point bank</div><strong class="universal-points">${state.universalPoints}</strong><small class="roster-meta">Move flexible points into any class goal.</small></div><select class="search-input"><option>Second Recess</option><option>Class Pet</option><option>Field Trip</option></select><input class="search-input points-input" value="5"><button class="btn btn-primary" data-toast="5 universal points assigned locally.">Assign</button></article><article class="panel milestone-list"><div class="eyebrow">Upcoming</div><h3>Class Milestones</h3>${[['🎮','Choice Time','75 shared points'],['📚','Extra Read-Aloud','100 shared points'],['🐉','Dragonswood Celebration','150 shared points']].map(m=>`<div class="milestone-row"><span class="text-23">${m[0]}</span><div><b>${m[1]}</b><small>${m[2]}</small></div><span class="muted lock-label">Locked</span></div>`).join('')}</article></section>`}
function goalCard(icon,kicker,title,count,pct){return `<article class="panel goal-card"><div class="goal-top"><div class="row"><span class="metric-icon">${icon}</span><div><div class="eyebrow">${kicker}</div><h3>${title}</h3></div></div><b class="gold">${count}</b></div><progress class="dw-progress" max="100" value="${pct}" aria-label="${title} progress">${pct}%</progress><div class="goal-controls"><button class="btn btn-secondary btn-sm" data-toast="Goal +1 simulated.">+1</button><button class="btn btn-secondary btn-sm" data-toast="Goal +2 simulated.">+2</button><button class="btn btn-secondary btn-sm" data-toast="Goal +5 simulated.">+5</button><button class="btn btn-secondary btn-sm" data-toast="Goal −1 simulated.">−1</button></div></article>`}

function passesPage(){return `${pageBanner('🎟️','Student movement','Pass Control','Approve, deny, return, and review passes without duplicate status changes.','<button class="btn btn-secondary btn-sm" data-toast="Pass history opened.">History</button>')}<section class="metric-grid">${metric('⏳','Pending',state.pendingPasses.length,'Needs a teacher decision')}${metric('🚶','Active',state.activePasses.length,'Currently out of class')}${metric('✓','Returned','14','Today')}${metric('⚠️','Needs attention','0','No stuck requests')}</section><section class="pass-columns"><article class="panel pass-list"><div class="section-heading no-top"><div><div class="eyebrow">Queue</div><h2>Pending Requests</h2></div></div>${state.pendingPasses.length?state.pendingPasses.map((p,i)=>`<div class="pass-card"><div class="pass-row"><div class="pass-student"><span class="roster-avatar">${p.name[0]}</span><div><div class="pass-kind">${p.kind} pass</div><b>${p.name}</b><p>Requested ${p.time} • student is waiting</p></div></div><div class="pass-actions"><button class="btn btn-primary btn-sm" data-pass-approve="${i}">Approve</button><button class="btn btn-danger btn-sm" data-pass-deny="${i}">Deny</button></div></div></div>`).join(''):'<div class="empty-center"><div>✓<strong>No students waiting.</strong></div></div>'}</article><article class="panel pass-list"><div class="section-heading no-top"><div><div class="eyebrow">Live</div><h2>Active Passes</h2></div></div>${state.activePasses.length?state.activePasses.map((p,i)=>`<div class="pass-card active-pass"><div class="pass-row"><div class="pass-student"><span class="roster-avatar">${p.name[0]}</span><div><div class="pass-kind">${p.kind} pass</div><b>${p.name}</b><p>Approved ${p.time} • currently active</p></div></div><button class="btn btn-secondary btn-sm" data-pass-return="${i}">Mark Returned</button></div></div>`).join(''):'<div class="empty-center"><div>✦<strong>No active passes.</strong></div></div>'}</article></section>`}

function jobsPage(){const jobs=[['🧹','Floor Warden','3 Gold per completed week','Caleb','4 / 4 days'],['📚','Lore Keeper','8 Gold per completed week','Aliya','4 / 4 days'],['💻','Tech Mage','5 Gold per completed week','Damian','4 / 4 days'],['🚪','Gate Keeper','4 Gold per completed week','Grayson','4 / 4 days'],['🌱','Garden Guardian','2 Gold per completed week','Nala','3 / 4 days'],['📦','Supply Smith','6 Gold per completed week','Raul','4 / 4 days']];return `${pageBanner('🧹','Classroom responsibility','Guild Jobs & Payroll','Assign every role, see weekly completion, and handle payroll from one roster.','<button class="btn btn-primary btn-sm" data-toast="Payroll preview recalculated.">Run Payroll Preview</button>')}<section class="metric-grid">${metric('🛡️','Jobs filled','6 / 6','Every role assigned')}${metric('✅','Completed','27','This week')}${metric('🪙','Payroll due','28','Gold on Friday')}${metric('⚠️','Needs check-in','2','Missed yesterday')}</section><section class="split-main"><article class="panel job-roster"><div class="eyebrow">Assignments</div><h3>Guild Roster</h3>${jobs.map(j=>`<div class="job-row"><div class="job-title"><span>${j[0]}</span><div><b>${j[1]}</b><small>${j[2]}</small></div></div><select><option>${j[3]}</option>${students.filter(s=>s.name!==j[3]).slice(0,4).map(s=>`<option>${escapeHtml(s.name)}</option>`).join('')}</select><div class="job-complete">${j[4]}</div></div>`).join('')}</article><aside class="panel payroll-panel"><div class="eyebrow">This week</div><h3>Payroll Preview</h3><div class="payroll-total"><small class="roster-meta">Total scheduled</small><strong>28 Gold</strong></div>${[['Caleb','Floor Warden','5 Gold'],['Kinsley','Lore Keeper','5 Gold'],['Joshua','Tech Mage','5 Gold'],['Alaina','Gate Keeper','5 Gold'],['Nala','Garden Guardian','2 Gold']].map(p=>`<div class="payroll-row"><div class="payroll-person"><span class="roster-avatar">${p[0][0]}</span><div><b>${p[0]}</b><small>${p[1]}</small></div></div><b class="gold text-9">${p[2]}</b></div>`).join('')}<button class="btn btn-primary" data-payroll ${state.payrollApproved?'disabled':''}>${state.payrollApproved?'✓ Friday Payroll Approved':'Approve Friday Payroll'}</button><div class="center muted prototype-note">Prototype only — no Gold will be issued.</div></aside></section>`}

function schedulePage(){return `${pageBanner('🕘','Classroom rhythm','Schedule & Calendar','Keep the day and class events visible together.','<div class="row"><button class="btn btn-primary btn-sm" data-toast="New block editor opened.">＋ Add Block</button><button class="btn btn-secondary btn-sm" data-toast="New event editor opened.">＋ Add Event</button></div>')}<section class="schedule-grid"><article class="panel timeline-teacher"><div class="section-heading no-top"><div><div class="eyebrow">${state.scheduleDay}</div><h2>Daily Timeline</h2></div><div class="filter-tabs">${['Today','Tomorrow'].map(d=>`<button class="filter-tab ${state.scheduleDay===d?'active':''}" data-schedule-day="${d}">${d}</button>`).join('')}</div></div>${[['🌅','Morning Math','8:00–8:25'],['📚','ELA','8:30–10:00'],['🍎','Recess / Lunch','10:00–11:00'],['🔭','Science','11:05–12:00'],['🏛️','Social Studies','12:05–1:00'],['🎨','Specials','1:10–2:10']].map(b=>`<div class="timeline-teacher-row"><span class="metric-icon schedule-mini-icon">${b[0]}</span><div><b>${b[1]}</b><small>${b[2]}</small></div><button class="btn btn-quiet btn-sm" data-toast="Schedule block edit simulated.">•••</button></div>`).join('')}<div class="row row-end mt-10"><button class="btn btn-secondary btn-sm" data-toast="Day copied locally.">Copy to another day</button><button class="btn btn-primary btn-sm" data-toast="Schedule saved locally.">Save Today</button></div></article><article class="panel calendar-card"><div class="eyebrow">Class calendar</div><h3>August 2026</h3><div class="calendar-grid">${['S','M','T','W','T','F','S'].map(d=>`<span class="head">${d}</span>`).join('')}${Array.from({length:35},(_,i)=>{const d=i-4;return `<span class="${d===24?'today':''} ${d===27?'selected':''}">${d>0&&d<32?d:''}</span>`}).join('')}</div><div class="calendar-events">${[['🏐','Volleyball Game','Aug 25 • 4:30 PM'],['📸','Fall Pictures','Aug 29'],['🌙','No-school day','Coming up']].map(e=>`<div class="event-row"><div>${e[0]}</div><div><b>${e[1]}</b><small>${e[2]}</small></div></div>`).join('')}</div></article></section>`}

function toolsPage(){const groups=[['Projection & Focus',[['⏱️','Visual Timer','Launch a large classroom timer.'],['🔊','Volume Meter','Give students a visible sound-level target.'],['🎯','Focus Screen','Project one clean direction at a time.']]],['Random & Groups',[['🎲','Random Scholar','Choose fairly from the current class roster.'],['👥','Group Maker','Create balanced temporary groups.'],['🔢','Number Picker','Pick a random number or range.']]],['Classroom Helpers',[['📝','Quick Poll','Run a fast teacher-led pulse check.'],['🎵','Ambient Sound','Launch calm, optional classroom ambience.'],['📣','Attention Signal','Project a quick visual attention cue.']]]];return `${pageBanner('🧰','Teacher utilities','Classroom Tools','Launch common tools quickly without turning the page into a wall of buttons.')} ${groups.map(g=>`<div class="section-heading"><div><div class="eyebrow">${g[0]}</div><h2>${g[0]}</h2></div></div><section class="tools-grid">${g[1].map(t=>`<article class="panel tool-card"><div class="tool-icon">${t[0]}</div><h3>${t[1]}</h3><p>${t[2]}</p><button class="btn btn-secondary btn-sm" data-toast="${t[1]} launched in prototype mode.">Launch</button></article>`).join('')}</section>`).join('')}`}

function leaderboardsPage(){const leaders=[['1','K','Kinsley','1,840 XP'],['2','J','Joshua','1,720 XP'],['3','N','Nala','1,610 XP'],['4','A','Alaina','1,590 XP'],['5','C','Caleb','1,530 XP']];return `${pageBanner('🏆','Positive recognition','Leaderboard Command','Celebrate class effort while keeping reward rules explicit and repeat-safe.','<div class="filter-tabs"><button class="filter-tab active">This Week</button><button class="filter-tab">All Time</button></div>')}<article class="panel teacher-podium"><div class="podium-person second"><div class="podium-avatar">K</div><b>Kinsley</b><small>1,840 XP</small><div class="podium-block">2</div></div><div class="podium-person first"><div class="podium-avatar">J</div><b>Joshua</b><small>1,920 XP</small><div class="podium-block">1</div></div><div class="podium-person third"><div class="podium-avatar">N</div><b>Nala</b><small>1,730 XP</small><div class="podium-block">3</div></div></article><section class="split-main mt-12"><article class="panel leader-table"><div class="row between"><div><div class="eyebrow">This week</div><h3>XP Standings</h3></div><button class="btn btn-primary btn-sm" data-reward-leaders>Reward top scholars</button></div>${leaders.map(l=>`<div class="leader-row"><div class="rank-num">#${l[0]}</div><div class="roster-avatar">${l[1]}</div><b>${l[2]}</b><span class="gold text-10">${l[3]}</span></div>`).join('')}</article><aside class="panel reward-rules"><div class="eyebrow">Reward rules</div><h3>Recognition</h3>${[['🥇','1st Place','Bonus badge + 20 XP'],['🥈','2nd & 3rd','Bonus badge + 10 XP'],['📚','Reading streak','Class shout-out'],['🌱','Biggest growth','Class shout-out']].map(r=>`<div class="reward-rule">${r[0]} <b>${r[1]}</b><small>${r[2]}</small></div>`).join('')}<div class="selected-badge mt-12">Duplicate rewards blocked</div></aside></section>`}

function arcadePage(){
  const selected=students.filter(s=>state.selected.has(s.id));
  const rows=selected.map(s=>{
    const a=state.arcadeRows[s.id]||{},criteria=a.criteria||{},tokens=Math.max(0,Math.min(3,Number(a.tokens)||0));
    const previewSession=window.DWV33Integration?.environment==='manual-preview'&&(a.sessionId||a.lastSessionId)?`<small>Preview refund ID: <code>${escapeHtml(a.sessionId||a.lastSessionId)}</code></small>`:'';
    return `<div class="pass-card"><div class="pass-row"><div class="pass-student"><span class="roster-avatar">${escapeHtml(s.name[0]||'?')}</span><div><b>${escapeHtml(s.name)}</b><p>${tokens} / 3 Tokens • ${a.active?'Session active':a.teacherEnabled?'Arcade open':'Arcade locked'}</p>${previewSession}</div></div><div class="row"><span class="selected-badge">${criteria.ready?'✓':'○'} Ready</span><span class="selected-badge">${criteria.responsible?'✓':'○'} Responsible</span><span class="selected-badge">${criteria.complete?'✓':'○'} Complete</span><button class="btn btn-secondary btn-sm" data-arcade-individual="${escapeHtml(s.id)}" data-enabled="${a.individualEnabled===false?'true':'false'}">${a.individualEnabled===false?'Unlock':'Lock'}</button></div></div></div>`;
  }).join('');
  return `${pageBanner('🕹️','Free-time currency','Arcade Time Command','Award up to three Tokens per period and control Arcade like a field trip or second recess.','<button class="btn btn-secondary btn-sm" data-arcade-refresh>↻ Refresh selected</button>')}<section class="panel teacher-form"><div class="form-grid"><div class="field"><label for="arcade-period">Period ID</label><input id="arcade-period" value="${escapeHtml(state.arcadePeriodId)}" maxlength="48"></div><div class="field span-3"><label>Class availability</label><div class="row"><button class="btn btn-primary" data-arcade-class="true" ${state.arcadeBusy?'disabled':''}>Open Arcade Time</button><button class="btn btn-danger" data-arcade-class="false" ${state.arcadeBusy?'disabled':''}>Lock Arcade Now</button></div></div></div><div class="eyebrow mt-12">Award one Token criterion to every selected scholar</div><div class="row mt-12">${[['ready','Ready'],['responsible','Responsible'],['complete','Complete']].map(([id,label])=>`<button class="btn btn-secondary" data-arcade-award="${id}" ${!selected.length||state.arcadeBusy?'disabled':''}>＋ ${label}</button>`).join('')}</div><p class="muted prototype-note">Each criterion can be awarded once per Phoenix school date and period. A full 3-Token wallet cannot stockpile more.</p></section><section class="panel pass-list mt-12"><div class="row between"><div><div class="eyebrow">Selected scholars</div><h3>${selected.length} selected</h3></div><button class="btn btn-secondary btn-sm" data-page="student-command">Change selection</button></div><div class="stack mt-12">${rows||'<div class="empty-center"><div><strong>Select scholars first.</strong><small>Use Student Command, then return here.</small></div></div>'}</div></section><section class="panel teacher-form mt-12"><div class="eyebrow">Technical issue only</div><h3>One-time session refund</h3><p class="muted">Select exactly one scholar, paste the audited session ID, and give a reason. The server caps the wallet at 3 and blocks a second refund.</p><div class="form-grid"><div class="field"><label for="arcade-refund-session">Session ID</label><input id="arcade-refund-session" placeholder="Arcade session document ID"></div><div class="field"><label for="arcade-refund-reason">Reason</label><input id="arcade-refund-reason" placeholder="Technical issue"></div><div class="field"><label>&nbsp;</label><button class="btn btn-danger" data-arcade-refund ${selected.length===1&& !state.arcadeBusy?'':'disabled'}>Review Refund</button></div></div></section>`;
}

function authGate(){const status=integrationSession.status||'loading',message=integrationSession.message||'Checking Teacher Command access…',canSignIn=status==='signed-out'||status==='unauthorized'||status==='error';return `<div class="portal teacher-shell" data-tester-build="v3.3"><main class="teacher-main" id="page-content"><div class="teacher-content"><section class="panel tool-card"><div class="eyebrow">SECURE INTEGRATION CANDIDATE</div><div class="tool-icon">🛡️</div><h3>${status==='unauthorized'?'Teacher access denied':'Teacher Command Sign In'}</h3><p>${escapeHtml(message)}</p>${canSignIn?'<button class="btn btn-primary" data-signin>Sign in with Google</button>':''}<p class="muted prototype-note">${escapeHtml(window.DWV33Integration?.environment||'loading')} • no production writes enabled</p></section></div></main><div class="tester-ribbon">V3.3 INTEGRATION • SAFE MODE</div></div>`}
function render(){state.page=currentPage();if(integrationSession.status!=='authorized'){app.innerHTML=authGate();bindAuthGate();document.title='[INTEGRATION] Dragonswood Command | Sign In';return}app.innerHTML=shell();bind();document.title=`[TESTER] Dragonswood Command | ${teacherNavItems().find(n=>n[0]===state.page)[2]}`}
function bindAuthGate(){app.querySelector('[data-signin]')?.addEventListener('click',async()=>{try{await integrationController?.signIn()}catch(err){showToast(`Sign-in failed: ${err?.code||err?.message||err}`)}})}
function teacherAccountDialog(){openDialog('Teacher Account',`<p>${escapeHtml(integrationSession.user?.email||'Authorized teacher')}</p>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn btn-primary" id="teacher-signout">Sign out</button>`);dialogRoot.querySelector('#teacher-signout')?.addEventListener('click',async()=>{closeDialog();try{await integrationController?.signOut()}catch(err){showToast(`Sign-out failed: ${err?.message||err}`)}})}
function applyTeacherSession(session){teacherName=session.teacherName||'Mr. Evans';students=Array.isArray(session.students)?session.students:[];if(!state.selected.size){const defaults=window.DWV33Integration?.environment==='manual-preview'?['Jacob Preview']:['Alaina','Christian','Nala'];for(const wanted of defaults){const row=students.find(s=>s.name===wanted);if(row)state.selected.add(row.id)}}}

function bind(){
 app.querySelectorAll('[data-page]').forEach(el=>el.addEventListener('click',()=>location.hash=el.dataset.page));
 app.querySelectorAll('[data-toast]').forEach(el=>el.addEventListener('click',()=>showToast(el.dataset.toast)));
 app.querySelector('[data-reference]')?.addEventListener('click',showReference);
 app.querySelector('.teacher-account')?.addEventListener('click',teacherAccountDialog);
 app.querySelectorAll('[data-student]').forEach(el=>el.addEventListener('change',()=>{el.checked?state.selected.add(el.dataset.student):state.selected.delete(el.dataset.student);render()}));
 app.querySelector('[data-clear-selected]')?.addEventListener('click',()=>{state.selected.clear();render()});
 app.querySelector('[data-select-visible]')?.addEventListener('click',()=>{students.forEach(s=>state.selected.add(s.id));render()});
 app.querySelector('#student-search')?.addEventListener('input',e=>filterRoster(e.target.value));
 app.querySelectorAll('[data-command-tab]').forEach(el=>el.addEventListener('click',()=>{state.commandTab=el.dataset.commandTab;render()}));
 app.querySelectorAll('[data-grade-segment]').forEach(el=>el.addEventListener('click',()=>{state.gradeFilter=el.dataset.gradeSegment;render()}));
 app.querySelectorAll('[data-command]').forEach(el=>el.addEventListener('click',()=>reviewCommand(el.dataset.command)));
 app.querySelector('[data-review]')?.addEventListener('click',()=>openDialog('Review Changes',`<p>Select an action card first. No write can happen just because a card looked clickable.</p><p class="muted">Selected scholars: ${selectedNames().join(', ')}</p>`));
 app.querySelector('[data-launch-scribe]')?.addEventListener('click',()=>confirmScribe(true));app.querySelectorAll('[data-close-mission]').forEach(el=>el.addEventListener('click',()=>confirmScribe(false)));
 app.querySelectorAll('[data-points]').forEach(el=>el.addEventListener('click',()=>{state.classPoints=Math.max(0,state.classPoints+Number(el.dataset.points));showToast(`Class points now ${state.classPoints} in prototype.`);render()}));
 app.querySelectorAll('[data-pass-approve]').forEach(el=>el.addEventListener('click',()=>approvePass(Number(el.dataset.passApprove))));app.querySelectorAll('[data-pass-deny]').forEach(el=>el.addEventListener('click',()=>denyPass(Number(el.dataset.passDeny))));app.querySelectorAll('[data-pass-return]').forEach(el=>el.addEventListener('click',()=>returnPass(Number(el.dataset.passReturn))));
 app.querySelector('[data-payroll]')?.addEventListener('click',payrollReview);app.querySelectorAll('[data-schedule-day]').forEach(el=>el.addEventListener('click',()=>{state.scheduleDay=el.dataset.scheduleDay;render()}));
 app.querySelector('[data-reward-leaders]')?.addEventListener('click',()=>openDialog('Reward top scholars?',`<p>This prototype shows the required review step. Production must verify each scholar has not already received the same weekly reward.</p><div class="stack"><div class="pass-card">Joshua • +20 XP</div><div class="pass-card">Kinsley • +10 XP</div><div class="pass-card">Nala • +10 XP</div></div>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn btn-primary" data-close-dialog>Simulate reward</button>`));
 app.querySelector('#arcade-period')?.addEventListener('input',e=>{state.arcadePeriodId=e.target.value});
 app.querySelector('[data-arcade-refresh]')?.addEventListener('click',refreshArcadeTeacher);
 app.querySelectorAll('[data-arcade-award]').forEach(el=>el.addEventListener('click',()=>awardArcade(el.dataset.arcadeAward)));
 app.querySelectorAll('[data-arcade-class]').forEach(el=>el.addEventListener('click',()=>setArcadeClass(el.dataset.arcadeClass==='true')));
 app.querySelectorAll('[data-arcade-individual]').forEach(el=>el.addEventListener('click',()=>setArcadeIndividual(el.dataset.arcadeIndividual,el.dataset.enabled==='true')));
 app.querySelector('[data-arcade-refund]')?.addEventListener('click',reviewArcadeRefund);
}
async function runArcade(action,success){
 if(!arcadeTeacher?.enabled){showToast('Arcade writes are locked outside the fictional emulator.');return}
 state.arcadeBusy=true;render();
 try{await action();showToast(success)}catch(err){showToast(err?.message||'Arcade command failed.')}finally{state.arcadeBusy=false;render()}
}
async function refreshArcadeTeacher(){
 const chosen=students.filter(s=>state.selected.has(s.id));
 if(!chosen.length){showToast('Select at least one scholar first.');return}
 state.arcadePeriodId=(document.querySelector('#arcade-period')?.value||state.arcadePeriodId).trim()||'period-1';
 await runArcade(async()=>{const pairs=await Promise.all(chosen.map(async s=>[s.id,await arcadeTeacher.getState(s.id,state.arcadePeriodId)]));for(const [id,value] of pairs)state.arcadeRows[id]=value},'Arcade status refreshed.');
}
async function awardArcade(criterion){
 const ids=[...state.selected];state.arcadePeriodId=(document.querySelector('#arcade-period')?.value||state.arcadePeriodId).trim()||'period-1';
 if(!ids.length){showToast('Select at least one scholar first.');return}
 await runArcade(async()=>{await Promise.all(ids.map(uid=>arcadeTeacher.award(uid,criterion,state.arcadePeriodId)));const pairs=await Promise.all(ids.map(async uid=>[uid,await arcadeTeacher.getState(uid,state.arcadePeriodId)]));for(const [id,value] of pairs)state.arcadeRows[id]=value},`${criterion[0].toUpperCase()+criterion.slice(1)} Token criterion processed.`);
}
function setArcadeClass(enabled){
 const destination=window.DWV33Integration?.environment==='manual-preview'?'this browser’s isolated preview state':'the fictional Firebase emulator';
 openDialog(`${enabled?'Open':'Lock'} Arcade Time?`,`<p>${enabled?'The class may start Arcade when each scholar has 3 Tokens.':'Every active Arcade session will be stopped by the authoritative server lock.'}</p><p class="muted">This command writes only to ${destination}.</p>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn ${enabled?'btn-primary':'btn-danger'}" id="confirm-arcade-class">${enabled?'Open class access':'Lock class now'}</button>`);
 dialogRoot.querySelector('#confirm-arcade-class')?.addEventListener('click',async()=>{closeDialog();await runArcade(()=>arcadeTeacher.setAvailability(enabled),`Class Arcade Time ${enabled?'opened':'locked'}.`)});
}
async function setArcadeIndividual(uid,enabled){await runArcade(async()=>{await arcadeTeacher.setAvailability(enabled,uid);state.arcadeRows[uid]=await arcadeTeacher.getState(uid,state.arcadePeriodId)},`Scholar Arcade access ${enabled?'opened':'locked'}.`)}
function reviewArcadeRefund(){
 const uid=[...state.selected][0],sessionId=document.querySelector('#arcade-refund-session')?.value.trim(),reason=document.querySelector('#arcade-refund-reason')?.value.trim();
 if(!uid||!sessionId||!reason){showToast('Choose one scholar, session ID, and technical reason.');return}
 openDialog('Confirm technical refund?',`<p>This returns only the spent Tokens that fit below the 3-Token cap. The server records the reason and permanently blocks a second refund for this session.</p><p class="muted">Session: ${escapeHtml(sessionId)}<br>Reason: ${escapeHtml(reason)}</p>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn btn-danger" id="confirm-arcade-refund">Issue one-time refund</button>`);
 dialogRoot.querySelector('#confirm-arcade-refund')?.addEventListener('click',async()=>{closeDialog();await runArcade(async()=>{await arcadeTeacher.refund(uid,sessionId,reason);state.arcadeRows[uid]=await arcadeTeacher.getState(uid,state.arcadePeriodId)},'Technical refund recorded.')});
}
function filterRoster(q){const value=q.trim().toLowerCase();const list=value?students.filter(s=>s.name.toLowerCase().includes(value)):students;const target=document.querySelector('#roster-list');if(target)target.innerHTML=rosterRows(list);target?.querySelectorAll('[data-student]').forEach(el=>el.addEventListener('change',()=>{el.checked?state.selected.add(el.dataset.student):state.selected.delete(el.dataset.student);render()}))}
function reviewCommand(command){if(!state.selected.size){showToast('Select at least one scholar first.');return}const serious=/Pause|Reset/.test(command);openDialog('Review Before Applying',`<div class="selected-badge">${state.selected.size} students selected</div><h3 class="mt-12">${command}</h3><p class="muted">${selectedNames().join(', ')}</p><p>Production would show the exact effect for every selected scholar before anything changes.</p>${serious?'<div class="pass-card serious-warning">⚠️ This is a serious consequence and requires a second explicit acknowledgement.</div>':''}`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn ${serious?'btn-danger':'btn-primary'}" data-close-dialog>Simulate apply</button>`)}
function confirmScribe(active){if(active){openDialog('Launch writing mission?',`<p>Students will see <b>Morning Quickwrite</b>. This prototype will not write to Firebase.</p>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn btn-primary" id="confirm-launch">Launch locally</button>`);dialogRoot.querySelector('#confirm-launch').addEventListener('click',()=>{state.scribeActive=true;closeDialog();render();showToast('Writing mission launched locally.')})}else{state.scribeActive=false;showToast('Mission closed locally.');render()}}
function approvePass(i){const p=state.pendingPasses.splice(i,1)[0];if(!p)return;state.activePasses.push({...p,time:'just now'});showToast(`${p.name}'s ${p.kind.toLowerCase()} pass approved locally.`);render()}
function denyPass(i){const p=state.pendingPasses.splice(i,1)[0];if(!p)return;showToast(`${p.name}'s request denied locally.`);render()}
function returnPass(i){const p=state.activePasses.splice(i,1)[0];if(!p)return;showToast(`${p.name} marked returned locally.`);render()}
function payrollReview(){openDialog('Approve Friday Payroll?',`<p><b>5 scholars • 28 Gold scheduled</b></p><p class="muted">Production must block duplicate payroll and show exact totals before the write.</p>`,`<button class="btn btn-secondary" data-close-dialog>Cancel</button><button class="btn btn-primary" id="confirm-payroll">Approve simulation</button>`);dialogRoot.querySelector('#confirm-payroll').addEventListener('click',()=>{state.payrollApproved=true;closeDialog();render();showToast('Payroll approved in prototype only.')})}
function showReference(){const path=references[state.page];if(!path){openDialog('Reference discrepancy',`<p>The supplied teacher screenshot set does not contain a distinct visible Pass Control design. The file named <code>dragonswood-teacher-05-pass-control.jpg</code> visibly shows <b>Class Rewards & Goals</b>.</p><p>This Pass Control page therefore follows the Master Bible requirements and shared teacher visual system instead of inventing a fake “exact” source.</p>`);return}const overlay=document.createElement('div');overlay.className='reference-overlay';overlay.innerHTML=`<button class="btn btn-gold reference-close">Close reference</button><img src="${path}" alt="Approved reference screenshot for current teacher page">`;document.body.appendChild(overlay);overlay.querySelector('button').addEventListener('click',()=>overlay.remove())}
window.addEventListener('hashchange',()=>{if(integrationSession.status==='authorized')render()});
(async function bootstrapIntegration(){
 if(!window.DWV33Integration){integrationSession={status:'error',message:'Integration runtime did not load.'};render();return}
 integrationController=await window.DWV33Integration.startTeacher(session=>{integrationSession=session;if(session.status==='authorized')applyTeacherSession(session);render()});
})();
